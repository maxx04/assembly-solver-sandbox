# Eigene .gitignore fuer diese Sandbox - ersetzt die per Sparse-Checkout automatisch
# mitgekommene FreeCAD-Upstream-.gitignore (die u.a. /.vscode/ pauschal ausschloss,
# obwohl VS-Code-Tasks/Settings hier bewusst mitversioniert werden sollen - siehe
# .vscode/tasks.json "sandboxfreecad"). Deckt nur ab, was in DIESEM Sparse-Checkout
# (src/Mod/Assembly, src/3rdParty/OndselSolver) und den eigenen Sandbox-Tools
# (standalone-check/, patches/, update-sandbox.sh) tatsaechlich anfallen kann - nicht
# den riesigen Rest des FreeCAD-Monorepos (Mod/, doc/, contrib/ etc.), den es hier
# gar nicht gibt.

# Build-Ausgaben
build/
standalone-check/build/
# FCPROJECT-PATCH (2026-09-11): seit der Ordner-Umstrukturierung 2026-09-09 liegt fcstd-output/
# nicht mehr zentral, sondern je Testfall im eigenen Unterordner
# (standalone-check/kinematic-tests/test_*/fcstd-output/) - die alte, nur den zentralen Pfad
# treffende Regel darunter war dadurch stillschweigend wirkungslos geworden. "**" matcht
# beliebig viele Verzeichnisebenen dazwischen, deckt also auch kuenftige, noch tiefer
# verschachtelte Testfaelle ab.
standalone-check/kinematic-tests/**/fcstd-output/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
compile_commands.json
Makefile

# Compiler-/Editor-Artefakte
*.o
*.obj
*.pyc
__pycache__/
*.orig
*~
*.clangd
.DS_Store

# FreeCAD-Backup-Dateien, die beim (Re-)Speichern automatisch entstehen - betrifft u.a.
# standalone-check/kinematic-tests/fixtures/ (git-getrackte Ausgangs-.FCStd-Dateien, siehe
# Projekt-Memory "reference-nested-fixture-methodology") - nur die eigentliche .FCStd soll
# getrackt werden, nicht die Backup-Kopie vom letzten Ueberschreiben.
*.FCBak

# Logs von update-sandbox.sh (falls spaeter analog zu FCProjects update-logs/ angelegt)
patches/update-logs/
