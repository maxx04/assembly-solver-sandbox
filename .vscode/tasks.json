{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "sandboxfreecad: configure",
      "detail": "CMake-Konfiguration fuer standalone-check/ (Compile-Check der Sandbox-Aenderungen, siehe standalone-check/CMakeLists.txt)",
      "type": "shell",
      "command": "cmake",
      "args": ["-S", "standalone-check", "-B", "standalone-check/build"],
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": [],
      "presentation": { "reveal": "silent", "panel": "shared", "clear": true }
    },
    {
      "label": "sandboxfreecad",
      "detail": "Baut die in der Sandbox geaenderten Assembly-Dateien gegen die echten FreeCAD-Header (kein Linking, keine Installation, siehe standalone-check/CMakeLists.txt)",
      "type": "shell",
      "command": "cmake",
      "args": ["--build", "standalone-check/build"],
      "options": { "cwd": "${workspaceFolder}" },
      "dependsOn": ["sandboxfreecad: configure"],
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": ["$gcc"],
      "presentation": { "reveal": "always", "panel": "shared", "clear": true }
    }
  ]
}
